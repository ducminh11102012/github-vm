name: macOS SSHX

on:
  workflow_dispatch:

jobs:
  sshx:
    runs-on: ghcr.io/cirruslabs/macos-runner:tahoe

    steps:
      - name: Install sshx
        run: |
          curl -sSf https://sshx.io/get | sh

      - name: Run sshx
        run: |
          sshx
