name: macOS SSHX

on:
  workflow_dispatch:

jobs:
  sshx:
    runs-on: macos-26

    steps:
      - name: Install sshx
        run: |
          curl -sSf https://sshx.io/get | sh

      - name: Run sshx
        run: |
          sshx
